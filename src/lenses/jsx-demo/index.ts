/**
 * JSX Demo Lens Barrel Export
 */

import { name } from './name.js';
import lens from './lens.js';
import register from './wc.js';
import config from './config.js';

// Default export only (generic object interface)
export default {
	name,
	lens,
	register,
	config,
};
