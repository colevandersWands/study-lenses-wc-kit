// see register.js from lenses:  export  a function that registers the component.js function behind a web component
// tag naming convention: `sl-ui-${name}`

export const register = () => {};

export default register;
