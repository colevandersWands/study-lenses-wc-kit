/**
 * UI Components Registration
 * Register all UI components for browser use
 */

import runRegister from './run/register.js';
import studyBarRegister from './study-bar/register.js';

// Register all UI components
runRegister();
studyBarRegister();
